print("There were bells, on a hill.")
print("And I never heard them ringing, no I never heard them at all")

import test

r = 1
print('radius is',r, 'circumference = ',test.func_circum(r), 'and', 'surface area is ', test.func_area(r))
